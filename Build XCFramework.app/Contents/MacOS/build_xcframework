#!/bin/bash

# Get the directory containing the .app bundle (which is the project directory)
PROJECT_DIR="$(dirname "$(dirname "$(dirname "$(dirname "$0")")")")"

# Open Terminal and run the build script
osascript -e "
tell application \"Terminal\"
    activate
    do script \"cd '$PROJECT_DIR' && chmod +x ./build_xcframework.sh && ./build_xcframework.sh\"
end tell
"